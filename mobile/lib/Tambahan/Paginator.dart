import 'package:flutter/cupertino.dart';

Widget Paginator() {
  return Container(
    child: Row(
      children: [
        // for prev
        // numbers (can be clicked)
        // for next
      ],
    ),
  );
}